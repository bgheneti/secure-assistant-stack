# -----------------------------------------------------------------------------
# ALLOWLIST — the human-readable "what the assistant may reach" file.
# One domain per line. A leading dot matches subdomains too.
# Everything NOT listed here is DENIED (default-deny) by squid.conf.
# After editing: `docker compose restart squid`
# -----------------------------------------------------------------------------

# --- TEE model provider (PrivateMode) — required for the model path ----------
# These are reached by the privatemode-proxy via CONNECT (no bump = attested).
api.privatemode.ai
cdn.confidential.cloud

# --- Google (Gmail + Calendar) — OAuth + API ---------------------------------
.googleapis.com

# --- Amazing Marvin ----------------------------------------------------------
serv.amazingmarvin.com

# --- Add new tool APIs below as you grow (one per line) ----------------------
# api.github.com
# slack.com
# .openai.com
cgr.dev

# --- Sandbox image build (in-DinD) -------------------------------------------
# Dockerfile.sandbox is built INSIDE dind-unrestricted by bring-up.sh step 7.
# That build needs the registries/CDNs below. If you instead build in CI and
# publish to ghcr.io (the recommended prod path — see Dockerfile.sandbox header),
# DELETE this block and keep only `ghcr.io` for the pull.
registry-1.docker.io                    # Docker Hub registry (dind pulls tool images)
auth.docker.io                          # Docker Hub token auth (registry-1 needs this)
production.cloudflare.docker.com        # Docker Hub blob CDN
cgr.dev                                # wolfi-base image metadata
.r2.cloudflarestorage.com              # Chainguard image blob CDN (signed URLs)
packages.wolfi.dev                     # apk packages
packages.cgr.dev                       # apk packages (mirror)
registry.npmjs.org                     # pnpm@10 via npm
raw.githubusercontent.com              # rtk install script
github.com                             # rtk release tarball (302 redirect host)
objects.githubusercontent.com          # rtk release blob CDN (legacy)
release-assets.githubusercontent.com   # rtk release blob CDN (current)

# --- DEFERRED (uncomment when you add Tinfoil as a fallback model provider) --
# inference.tinfoil.sh
# .tinfoil.sh
